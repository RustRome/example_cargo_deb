-- Your SQL goes here
create table event (
    id  serial PRIMARY KEY,
    title varchar not null
);
-- Your SQL goes here
create table food (
    id  serial PRIMARY KEY,
    food varchar not null
);
